/** 
 * Ubuntu/Debian Essential Packages (libs)
 */

$ apt-get update
$ apt-get install build-essential libgtk2.0 libgtk2.0-dev


/**
 * Archlinux Essential Packages (libs)
 */

$ pacman -Syu
$ pacman -S base-devel 


/**
 * Compile (from Shell)
 */ 

$ gcc -Wall -o gwriter gwriter.c `pkg-config --cflags --libs gtk+-2.0`



/**
 * Compile (With Geany IDE)
 */

1) Choose more build actions
2) Set build menu commands
3) gcc -Wall -o "%e" "%f" `pkg-config --cflags --libs gtk+-2.0`


/**
 * Autoconfig & AutoMake
 */

$ autoreconf --install

$ ./configure
$ make
